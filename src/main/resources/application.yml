spring:
  application:
    name: projectManagement

#  DB Connection related configuration
  datasource:
    url: jdbc:postgresql://localhost:5433/projectManagement
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8000

jwt:
  secret: wefnjsdkcvnjekrawsfndocwneqoafdnoq
  expiration: 86400000 # 1 Day in ms

logging:
  level:
    org.springframework.security: DEBUG